1. cards_f: 编写get_bigger_fks函数
2. and_or_tree
3. 可以优化的地方：
   1) 如果一个节点产生的所有子节点的对方剩牌与另一个节点完全相同，那么另一个节点可以剪枝。

Q：剪枝(基本算法步骤一中的4 b))有没有可能影响算法的结论？
即：如果Sj1=Sj2，j2后产生，则将j2将不被放到open_table和closed_table，j2也不会在标示过程中使用到，这样会不会影响算法结果？
已有结论是：
1) 如果j1是j2的间接父节点，那么j1和j2的S值不可能相同；
2) 如果j1和j2是不同类型的节点（如j1是或节点，j2是与节点），其S值不可能相同；
3) 假设j1和j2是相同类型的节点，其共同的父节点是：N。j1经过A1A2...Ar1到达N，j2经过B1B2...Br2到达N。
最简单的情况是：j1经过A1，j2经过B1B2B3，则FB2=""，即FA1=FB1+FB3(例如：N出牌33到达j1，N出牌3不要再出3到达j2）
